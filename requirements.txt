# Comms
pymavlink

# Vision — need at least one backend
opencv-python
numpy
onnxruntime          # for best.onnx  (use onnxruntime-gpu if CUDA available)
ultralytics          # for best_3_.pt (optional if using ONNX only)

# Debug
matplotlib
